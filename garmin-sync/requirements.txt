# Pinned. The 0.2.x line used `garth`, whose login broke in March 2026 (garth deprecated).
# 0.3.x uses Garmin's mobile-SSO flow (curl_cffi). Verify still-working before bumping.
garminconnect==0.3.5
